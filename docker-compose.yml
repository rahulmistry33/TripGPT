version: '3.8'

services:
  trip-gpt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trip-gpt-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
